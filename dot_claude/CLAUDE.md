# Global Instructions

## Communication
- Respond in Japanese
- Commit messages and code comments in English

## Workflow
- Read existing code before editing
- Keep changes minimal and focused on the current request
- Match the existing style of the repository
- Never silently ignore errors — surface them explicitly
- Prefer non-interactive commands (e.g. `git --no-pager diff`)

## Information Lookup
- When you need factual information, documentation, or context: always search via WebSearch or Obsidian MCP (search_notes) first — do not rely on training data alone
- Run searches in parallel (e.g. WebSearch and Obsidian search_notes at the same time) to minimize latency
- Obsidian vault uses PARA structure: 01_Projects, 02_Areas, 03_Resources, 04_Archive
- Check Obsidian for project context, personal notes, and prior research before starting work
